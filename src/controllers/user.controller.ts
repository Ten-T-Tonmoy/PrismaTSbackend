import { Request, Response } from "express";

import { userServcies } from "../services/user.services";


export const userController={
    getAllusers: async(req:Request,res:Response)=>{
        try
    }
}
